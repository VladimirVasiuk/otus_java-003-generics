package org.edu;

import java.util.Objects;

public class Main {


    public static void main(String[] args) {
        // TODO: создайте класс Pair, чтобы все следующие строки компилировались и выполнянлись корректно

        //Pair<String, Integer> pair = new Pair<String, Integer>("Hello", 12);

        //assert Objects.equals("Hello", pair.getLeft());

        //assert Objects.equals(12, pair.getRight());

        //assert !pair.equals(new Pair<String, Long>("Hello", 12L));

        //pair = Pair.of("World", "!");
    }
}
